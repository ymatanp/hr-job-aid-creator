"""HR Job Aid Creator — web-app backend package.

Wraps the same logic the Claude Code agent uses (source ingestion, Claude
reasoning, and python-pptx template filling) so it can run behind a Streamlit UI.
"""
